
  name: todolist
  description: "A Todo app with Bloc/Cubit"
  publish_to: 'none'
  version: 1.0.0+1

  environment:
    sdk: '>=3.0.0 <4.0.0'

  dependencies:
    flutter:
      sdk: flutter
    flutter_bloc: ^8.1.6
    equatable: ^2.0.5
    cupertino_icons: ^1.0.8
    uuid: ^4.5.1

  dev_dependencies:
    flutter:
      sdk: flutter
    flutter_lints: ^6.0.0

  flutter:
    uses-material-design: true
